import { ImportHistoryPage } from "@/features/ingestion/components/import-history-page";

export const metadata = {
  title: "Import History",
};

export default function ImportsHistoryPage() {
  return <ImportHistoryPage />;
}
